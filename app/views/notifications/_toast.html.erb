<script>
document.dispatchEvent(
  new CustomEvent("notification:received", {
    detail: {
      message: "<%= j(message) %>",
      type: "<%= type %>",
      url: "<%= url %>"
    }
  })
)
</script>