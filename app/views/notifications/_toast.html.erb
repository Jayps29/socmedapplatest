<script>
document.dispatchEvent(
  new CustomEvent("notification:received", {
    detail: {
      message: "<%= j(message) %>",
      type: "<%= type %>",
      url: "<%= url %>",
      friendRequestId: <%= friend_request_id.present? ? friend_request_id : "null" %>
    }
  })
)
</script>